import React, { FC } from 'react'

interface AboutProps {

}

export const About: FC<AboutProps> = (props: AboutProps) => {
    const { } = props

    // Hero Image
    // About Section
    // Services
    // Gallary
    return (
        <div>
            About Section
        </div>
    )
}